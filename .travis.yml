language: node_js
node_js:
  - 'lts/*'
sudo: false
cache: yarn
before_install:
script:
  - yarn lint
  - yarn test-coverage
after_script:
  - npm install --global codecov coveralls
  - cat ./coverage/lcov.info | coveralls
  - cat ./coverage/lcov.info | codecov
